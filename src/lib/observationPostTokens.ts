/**
 * Shared design tokens for the unified observation post.
 * Used by ObservationPostCore (car page hero) and CarObservationFeedCard (feed preview).
 */
export const OBSERVATION_BG = "#0d141b";
export const OBSERVATION_ACCENT = "#34eab8";

export const oswald = { fontFamily: "'Oswald', 'Impact', sans-serif" } as const;
export const oswaldLight = {
  fontFamily: "'Oswald', sans-serif",
  fontWeight: 300,
} as const;
